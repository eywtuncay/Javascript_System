/*

 DOM (DOCUMENT OBJECT MODEL) Nedir?

DOM, ya da Document Object Model, bir web sayfasının yapısının ve içeriğinin bir modelidir. JavaScript kullanarak bu model üzerinden web sayfasının elemanlarına erişebilir, bu elemanları değiştirebilir, ekleyebilir veya kaldırabilirsiniz. DOM, bir web sayfasının HTML veya XML dokümanlarını temsil eden ve onlarla etkileşime geçmenizi sağlayan bir programlama arayüzüdür.

Sayfamın tamamı Document Objesi içindedir. Document Objesi de Window objesi içnerisindedir.

Window > Document > Head,Body....

*/



// console.log(window);

// let value ;

// console.log(document);

// value = document.location.href;
// value = document.location.hostname;
// value = document.location.port;
// value = document.location.pathname;


// value = document.all;
// value = document.characterSet;

// value = document.title;
// value = document.links.item(4);
// value = document.links.item(4).id;
// value = document.links.item(4).getAttribute("id");
// value = document.links.item(4).getAttribute("class");
// value = document.links.item(4).classList[3];

// value  = document.forms.item(1).children;

// value = document.domain;
// value = document.contentType;

// console.log(value);

